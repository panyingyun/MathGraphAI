# Fix001 — 代码审查问题修复记录

> 本次修复基于对仓库文档与代码的全面审查（含后端逐行审查子代理与前端逐行审查子代理），按严重级别顺序落地。
> 修复后：后端 `pytest` **181 passed**，前端 `vitest` **19 passed**，前端 `npm run build` 通过。

---

## 修复顺序总览

| # | 问题 | 严重级别 | 状态 |
|---|------|---------|------|
| 1 | H1 渐近线伪零点/伪交点 + `_bisection` 崩溃 | 高 | ✅ |
| 2 | H2 非流式 `/chat` 异常无兜底 → 500 + 幂等永久 409 | 高 | ✅ |
| 3 | M1 乐观锁非原子 + M5 NaN 绕过校验 + M3 固定采样窗口误拒 | 中 | ✅ |
| 4 | 前端会话竞态 H1/H2 + IME + tan(kx) 断线 + π 刻度无上限 | 高/中 | ✅ |
| 5 | M2/M4/M6/M7 + 死代码 + 文档配置漂移 | 中/低 | ✅ |

---

## 1. H1 — 渐近线伪零点/伪交点 + `_bisection` 崩溃

**文件**：`backend/app/utils/numeric_analysis.py`

**问题**：对 `1/(x-c)`、`tan(x)` 等含垂直渐近线的函数，采样跨过极点时 `y0*y1 < 0`，二分法收敛到极点并把极点当零点/交点返回，且 `_bisection` 直接 `fn(mid)` 未防护，极点恰落中点会抛 `ZeroDivisionError`。

**修复**：
- 新增模块常量 `_RESIDUAL_FACTOR = 1000`。
- `find_zeros` 在得到候选根后校验 `abs(y) > tol * _RESIDUAL_FACTOR` 时丢弃（极点残差巨大）。
- `_append_intersection_point` 校验 `residual > tol * _RESIDUAL_FACTOR` 时丢弃伪交点。
- `_bisection` 改用 `_safe_eval`，求值失败返回 `None`，不再上抛异常。

**回归测试**（`backend/tests/test_math_tools.py`）：
- `1/(x-0.025)` 零点数为 0。
- `tan(x)` 在 `[-10,10]` 只返回 7 个真实零点（`-3π..3π`），无极点。
- `find_intersections("tan(x)", "0")` 只返回真实零点，`residual < 1e-3`。

---

## 2. H2 — 非流式 `/chat` 异常无兜底

**文件**：`backend/app/routers/chat.py`、`backend/app/services/session_service.py`、`backend/app/main.py`

**问题**：非流式 `run_agent` 无 try/except，一旦异常 → 500，但 `agent_runs` 与用户消息已落库，run 永久停在 `running`、`response_json=None`；同 `request_id` 重试撞唯一约束 → 永久 409。流式路径有 `_close_unfinished_run` 收口，非流式没有。

**修复**：
- 非流式 `run_agent` 包 try/except，异常时先 `_close_unfinished_run`（run 收口为 error + 落库错误响应），再返回 500。
- 新增 `close_stale_runs`，应用启动（lifespan）时把遗留 `running` 状态的 run 收口为 `error`（`error_code=interrupted`），并写入可缓存的错误响应，避免幂等命中永久 409。

**回归测试**（`backend/tests/test_idempotency_revision.py`）：
- `test_nonstream_agent_exception_closes_run_and_retry_hits_cache`：`run_agent` 抛异常后返回 500、run 状态为 `error` 且 `finished_at` 非空；同 `request_id` 重试命中错误缓存（200 + `errorCode=agent_error`）而非 409。

---

## 3. M1 / M5 / M3

### M1 — 乐观锁原子化

**文件**：`backend/app/services/session_service.py`、`backend/app/routers/chat.py`、`backend/app/routers/sessions.py`

**问题**：`expected_revision` 只在请求开始预检，写回端无条件写、纯内存 bump，并发/重叠请求会静默丢更新。

**修复**：`persist_graph_state` 改为原子条件写（`UPDATE … WHERE revision = 加载值`），失败返回 `False`；三条写路径（chat 提交、`/commands`、`PATCH`）在 `False` 时回滚并返回 409。流式路径将 409 转为 SSE `error` 事件。

### M5 — NaN 绕过 viewport 校验

**文件**：`backend/app/schemas/graph.py`

**问题**：`Viewport.valid_ranges` 对 NaN 的比较恒 False，NaN/Inf 可被接受并落库污染画布。

**修复**：校验器开头对四个字段逐个做 `math.isfinite` 检查。

**回归测试**（`backend/tests/test_security_limits.py`）：`test_rejects_nan_viewport`。

### M3 — 固定采样窗口误拒域外函数

**文件**：`backend/app/utils/equation_validator.py`

**问题**：`validate_expression` 只对 `[-10,10]` 采样，`sqrt(x-12)`、`log(x-20)` 被误拒。

**修复**：近窗无有限值时二次探测 `[-1e6, -1000, -100, 100, 1000, 1e6]`，任一有限即放行；仍无有限值才拒绝。

**回归测试**：`test_accepts_domain_shifted_function`。

---

## 4. 前端修复

### 会话竞态（H1/H2）

**文件**：`frontend/src/stores/appStore.ts`

**问题**：`sendMessage` 的 SSE 完成/回调不校验当前会话是否仍是发起会话，导致「切换会话后串台」「删除会话后被复活」。

**修复**：
- `onPhase`/`onStep` 回调加 `currentSession.id === 发起 id` 守卫。
- `await api.sendMessage(...)` 后加守卫，会话已切换/删除则丢弃迟到结果。
- `AbortError` / `revision_conflict` / 通用错误分支均按「是否仍为当前会话」条件更新 `currentSession`。

### IME 误发送

**文件**：`frontend/src/components/chat/ChatInput.tsx`

**修复**：`onKeyDown` 中若 `event.nativeEvent.isComposing || event.keyCode === 229` 则直接返回，避免中文输入法候选确认回车触发发送。

### tan(k·x) 断线位置错误

**文件**：`frontend/src/utils/graphSampler.ts`

**问题**：采样断线硬编码 `(n+1/2)π`，与虚线渐近线（`trigAxis.tanAsymptotePeriod`）用两套周期，`tan(2x)` 在零点被错误断开。

**修复**：`crossesHalfPiAsymptote` 增加 `period` 参数，`sampleFunction` 用 `tanAsymptotePeriod` 计算周期并传入，断线与虚线共用同一周期。

### π 刻度无上限循环

**文件**：`frontend/src/utils/trigAxis.ts`

**问题**：宽视口（±1e6）下 `buildPiAxisTicks` 生成约 63.7 万个刻度，卡顿数秒。

**修复**：加 `MAX_TICKS = 60` 上限，宽视口时把步长放大为 π 的整数倍，刻度数恒 ≤ 60。

**回归测试**：`graphSampler.test.ts`（自定义周期断线 + `tan(2*x)` 极点/零点）、`trigAxis.test.ts`（宽视口刻度上限）。

---

## 5. M2 / M4 / M6 / M7 + 死代码 + 文档

### M2 — 本地规划器数值异常未捕获

**文件**：`backend/app/agent/local_planner.py`

**修复**：新增 `_safe_numeric(call, default)`，包裹所有 `find_zeros`/`find_extrema`/`find_intersections`/`compare_functions` 裸调用，异常时返回安全空结果。

### M4 — 工具异常文本透传

**文件**：`backend/app/agent/executor.py`

**修复**：`_execution_error_failure` 的用户可见 `message` 改为通用文案，内部异常细节只写入 `observation.data["detail"]`。

### M6 — SQLite 并发写加固

**文件**：`backend/app/database.py`

**修复**：`create_engine` 加 `pool_pre_ping=True`；SQLite 连接事件里设置 `PRAGMA journal_mode=WAL`、`PRAGMA busy_timeout=5000`、`PRAGMA foreign_keys=ON`。

### M7 — 线程池

**说明**：曾尝试在 lifespan 关闭阶段 `shutdown_tool_pool()`，但这会关闭模块级共享线程池导致后续测试 `RuntimeError`，已回退。其崩溃根因已被 H1 消除；「超时后无法强停后台线程」是 `ThreadPoolExecutor` 固有限制，保留为已知限制（`_execute_with_timeout` 已通过隔离副本保证不污染主状态）。

### 死代码清理

- 删除 `backend/app/utils/prompt_builder.py`（整文件无引用）。
- 删除 `backend/app/agent/adapter.py::final_from_text`。
- 删除 `backend/app/services/session_service.py::delete_messages_by_request_id`。
- 删除 `backend/app/agent/policy.py` 中 `len(after.equations) < 0` 永假分支。

### 文档修正

- `docs/baseline/README.md`：将过时的「当前链路仍会在 DeepSeek 异常时静默降级」改为「已显式降级并标记 `fallbackUsed` 与错误码」。

---

## 测试结果

| 项 | 修复前 | 修复后 |
|----|--------|--------|
| 后端 `pytest` | 176 passed | **181 passed** |
| 前端 `vitest` | 16 passed | **19 passed** |
| 前端 `npm run build` | — | ✅ |

---

## 已知保留项（本次未处理）

- `backend/math_graph_ai.db` 是被误提交的二进制数据库（本次测试运行时被触碰，已 `git checkout` 恢复），建议后续加入 `.gitignore` 并从版本库移除。
- sw-design 与代码的颜色值漂移（`#ef4444`/`#22c55e` vs `#da3437`/`#007d55`）。
- `docs/baseline/metrics-live.json` 中残留的 Windows 绝对路径与过期数据。
- `Plan02.md` 中 `AGENT_*_ATTEMPTS=1` 与当前默认 `3` 的差异（`.env.example`/`config.py` 以 3 为准）。
- M7 的「超时无法强停后台线程」属线程池固有限制。
