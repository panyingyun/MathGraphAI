[pytest]
markers =
    contract: 模型契约 / StructuredResult 结构问题
    expression: 表达式白名单与求值问题
    state: GraphState 确定性更新问题
    persistence: 会话持久化 / API 集成问题
    fallback: DeepSeek 失败与本地降级问题
testpaths = tests
pythonpath = .
